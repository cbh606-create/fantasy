DATABASE_URL="file:./dev.db"
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
ESPN_LIVE=false
# Optional server-wide fallback when ESPN_LIVE=true.
# Preferred: each user pastes cookies via /roster → Connect ESPN.
ESPN_S2=
ESPN_SWID=
# Headed Playwright ESPN login connect (local only)
ESPN_CONNECT_LIVE=false
# Playwright only: bypass Clerk when the request hostname is localhost.
E2E_BYPASS_AUTH=false
